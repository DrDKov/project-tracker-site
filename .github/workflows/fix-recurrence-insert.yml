name: Fix recurrence insert
on:
  push:
    branches: [main]
    paths: ['.github/workflows/fix-recurrence-insert.yml']
  workflow_dispatch:
permissions:
  contents: write
jobs:
  fix:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: true
      - name: Apply recurrence insert fix
        run: python .github/scripts/fix_recurrence_insert.py
      - name: Check syntax
        run: |
          node --check assets/app-runtime.js
          node --check assets/app.js
      - name: Commit recurrence insert fix
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add -A
          git commit -m "Use insert for recurrence task instances [skip ci]" || exit 0
          git push
